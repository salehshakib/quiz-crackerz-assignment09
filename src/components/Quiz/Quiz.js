import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Quiz = () => {
    // const {quiz} = useLoaderData();
    return (
        <div>
            this is quiz page
        </div>
    );
};

export default Quiz;